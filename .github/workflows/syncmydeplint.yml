name: CI Lockfile Verification

on:
  pull_request:
    branches: [main]

jobs:
  verify-lockfile:
    name: Verify Lockfile Synchronization
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: nivinvysakh/syncmydep@Improvements
        with:
          check-only: 'true'
